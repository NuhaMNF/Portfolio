"use client";

import { DynamicIslandHeader } from "@/components/system/DynamicIslandHeader";

/**
 * Top-right Dynamic Island Header integrating Cyber Radio, 3D Background toggle, Key Glow Presets, Audio Mute & Theme Switch.
 */
export function NotebookStatus() {
  return <DynamicIslandHeader />;
}
